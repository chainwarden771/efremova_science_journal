# 📘 Техническая записка проекта: Science Journal Platform

## 1. Общая информация

Проект представляет собой SPA-платформу для публикации и чтения научных статей с серверной частью на Node.js + Express и клиентской частью на React.

Архитектура разделена на два основных слоя:

- **client/** — React SPA (Vite)
- **server/** — API + бизнес-логика + MongoDB + файловое хранилище

---

## 2. Используемый стек технологий

| Категория    | Технологии                    | Назначение                    |
| ------------ | ----------------------------- | ----------------------------- |
| Frontend     | React, React Router DOM       | SPA интерфейс                 |
| HTTP         | Axios                         | REST API взаимодействие       |
| Backend      | Node.js, Express              | API сервер                    |
| Database     | MongoDB (Mongoose/driver)     | Хранение данных               |
| Validation   | Zod                           | Валидация входных данных      |
| Auth         | JWT + Cookies                 | Авторизация и сессии          |
| Upload       | Multer (multipart/form-data)  | Загрузка файлов               |
| Realtime     | Socket.IO                     | (планируется/частично)        |
| FS           | fs/promises                   | Файловое хранилище            |
| Code quality | ESLint, Prettier              | Линтинг и форматирование      |
| Modules      | ESM (type: module)            | Современная модульная система |
| API style    | REST                          | CRUD операции                 |
| Pagination   | offset/limit                  | Пагинация данных              |
| Routing      | Express Router + React Router | Маршрутизация                 |
| Params       | URL params                    | Передача идентификаторов      |

---

## 3. Архитектура клиентской части (React SPA)

### 3.1 Структура (FSD-like)

```

client/src/
├── api/ # HTTP слой (axios instances)
│ └── app/
│ ├── session/
│ └── posts/
│
├── components/ # UI компоненты (Header, Modal, Post)
├── features/ # бизнес-логика (auth, posts)
├── pages/ # страницы (Auth, Explore, Publish...)
├── shared/
│ ├── UI/ # переиспользуемые UI компоненты
│ ├── hooks/ # кастомные хуки
│ ├── context/ # глобальные контексты (Profile, Modal)
│ ├── utils/ # работа с storage/cookies
│ └── consts/ # константы

```

### 3.2 Основные принципы

- UI отделён от бизнес-логики (features layer)
- Контексты используются для глобального состояния (user, modal)
- API слой изолирован через axios instances
- Логика авторизации вынесена в ProfileProvider

---

## 4. Архитектура серверной части

### 4.1 Структура

```

server/src/
├── config/ # конфигурации (MongoDB)
├── entities/ # модели (User, Post)
├── middleware/ # auth, session verify
├── routes/ # REST endpoints
│ ├── session/
│ └── posts/
├── services/ # бизнес-логика (DB abstraction)
└── validation/ # Zod схемы

```

---

## 5. API архитектура

### 5.1 Session API

```

POST /api/session/auth
POST /api/session/register
POST /api/session/logout
GET /api/session/check

```

### 5.2 Posts API

```

GET /api/posts/explore
GET /api/posts/:id
POST /api/posts/publish

```

---

## 6. Модель данных MongoDB

### User

```js
{
  fullname: String,   // max 45
  login: String,      // max 12
  password: String,   // max 45
  gender: "m" | "f",
  role: "p" | "r",
  posts: [ObjectId]
}
```

### Post

```js
{
  title: String,
  description: String,
  postID: ObjectId (unique),
  author: ObjectId,
  publishDate: Number,
  imageFileName: String,
  pdfFileName: String
}
```

---

## 7. Файловое хранилище

### Структура

```
server/file_storage/
└── posts/
    ├── <postId>.png
    ├── <postId>.pdf
```

### Принцип

- файлы связаны через `postId`
- расширение сохраняется отдельно
- доступ через express.static

```
app.use('/file_storage', express.static('./file_storage'));
```

---

## 8. Авторизация

### JWT + Cookies

1. сервер создаёт JWT
2. отправляет через cookie `sessionKey`
3. middleware `verifySession` проверяет токен
4. профиль пользователя доступен через контекст

---

## 9. Валидация

Используется **Zod**:

- регистрация
- авторизация
- создание постов

Пример:

```js
registerSchema.parse(req.body);
```

---

## 10. Upload система

### Multer

- multipart/form-data
- загрузка:
  - image
  - pdf

---

## 11. React Features Layer

### session

- useAuth
- useRegister

### posts

- usePost (CRUD)

---

## 12. API слой (Axios)

### Структура

```
api/app/
├── index.js (axios instance)
├── session/
└── posts/
```

### Особенности

- baseURL централизован
- interceptors (планируется расширение)
- cookies поддержка (withCredentials)

---

## 13. Реализованные паттерны

- FSD-like архитектура
- Context API вместо Redux
- Service layer (server)
- Feature-based hooks
- REST API separation
- Middleware auth pipeline

---

## 14. Потенциальные улучшения

(не критично, но планируется)

- WebSocket (Socket.IO)
- Redis caching
- полноценный state manager (Redux Toolkit)
- rate limiting
- file streaming
- CDN для файлов

---

## 15. Вывод

Проект построен по модульной архитектуре с разделением:

- UI (React SPA)
- бизнес-логика (features + services)
- backend API (Express)
- данные (MongoDB + FS)

Архитектура уже поддерживает масштабирование и добавление новых сервисов без изменения ядра системы.
